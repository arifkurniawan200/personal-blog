baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'Arif Kurniawan Personal Blogs'
theme = 'github-style'
lastmod = true
enableGitalk = false

[params]
  avatar = 'images/profile.png'
  headerIcon = "/images/GitHub-Mark-Light-32px.png"
  userStatusEmoji = "😀"
  author = 'Arif Kurniawan'
  github = 'Arifkurniawan200'
  Description = 'Hate Programming and Love Mobile Legend #RoadToMytic'
  location = 'South Jakarta'
  email = 'arifkurniawan200@outlook.co.id'
  linkedin = 'arifkurniawan0906'
  instagram = 'arifkurniawan0609'
  favicon = "/images/github.png"


[params.gitalk]
    clientID = "Your client ID" # Your client ID
    clientSecret = "Your client secret" # Your client secret
    repo = "LuvSia" # 您的博客的github地址Repository name，例如：xxxx.github.io
    owner = "ventusoon" # 您的GitHub ID
    admin = "ventusoon" # 您的GitHub ID
    id = "location.pathname" # 文章页面的链接地址就是ID
    labels = "gitalk" # Github issue labels. If you used to use Gitment, you can change it
    perPage = 15 # Pagination size, with maximum 100.
    pagerDirection = "last" # Comment sorting direction, available values are 'last' and 'first'.
    createIssueManually = true # 设置为true，如果是管理员登录，会自动创建issue，如果是false，需要管理员手动添加第一个评论(issue)
    distractionFreeMode = false # Enable hot key (cmd|ctrl + enter) submit comment.
    proxy = "https://cors-anywhere.azm.workers.dev/https://github.com/login/oauth/access_token"

[frontmatter]
  lastmod = ["lastmod", ":fileModTime", ":default"]